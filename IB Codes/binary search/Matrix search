int Solution::searchMatrix(vector<vector<int> > &a, int b) {
    int n=a.size();
    int m=a[0].size();
    int i=0,j=m-1;
    while(i>=0&&j>=0&&i<n&&j<m)
    {
        if(a[i][j]==b) return 1;
        else if(b>a[i][j]) i++;
        else j--;
    }
    return 0;
}
