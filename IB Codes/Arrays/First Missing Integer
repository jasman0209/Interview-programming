int Solution::firstMissingPositive(vector<int> &A) {
    sort(A.begin(),A.end());
    int n=A.size();
    int in=1;
    for(int i=0;i<n;i++)
    {
        if(A[i]==in)
        in++;
    }
    return in;
}
